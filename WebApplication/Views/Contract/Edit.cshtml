@model DataAccess.ViewModels.ContractEdit

@{ Layout = "~/Views/Shared/_Layout.cshtml"; }


<div class="row">
    <div class="col-md-1"> </div>
    <div class="col-md-4">
        <div class="box box-danger" style="margin-top:5%;">

            <div class="box-header">
                <h3 class="box-title">@ViewData["Title"]</h3>
                <div class="pull-right">
                    @Html.ButtonBackToList("Contract")
                </div>
            </div>

            <hr />

            <form asp-controller="Contract" asp-action="Edit" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="ContractMembership" />
                <input type="hidden" asp-for="ContractType" />
                <input type="hidden" asp-for="CreatedBy_Id" />
                <input type="hidden" asp-for="IsActive" />
                <input type="hidden" asp-for="CreatedBy_FullName" />
                <input type="hidden" asp-for="CreatedOn" />

                <div class="box-body">

                    <div class="form-group">
                        <label asp-for="Title" class="control-label"></label>
                        <div class="input-group">
                            <input asp-for="Title" class="form-control" readonly="readonly" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="DayOfDaysPerWeek" class="control-label"></label>
                        <div class="input-group">
                            <input asp-for="DayOfDaysPerWeek" class="form-control">
                            <span asp-validation-for="DayOfDaysPerWeek" class="text-danger"></span>
                        </div>
                    </div>


                    <div class="form-group">
                        <label asp-for="WorkingDaysPerWeek" class="control-label"></label>
                        <div class="input-group">
                            <input asp-for="WorkingDaysPerWeek" class="form-control" >
                            <span asp-validation-for="WorkingDaysPerWeek" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="HoursPerWeek" class="control-label"></label>
                        <div class="input-group">
                            <input name="HoursPerWeek" class="form-control" pattern="^\d*(\,\d{0,2})?$"  value="@Model.HoursPerWeek">
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="HoursPerDay" class="control-label"></label>
                        <div class="input-group">
                            <input name="HoursPerDay" pattern="^\d*(\,\d{0,2})?$" class="form-control" value="@Model.HoursPerDay">
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="GrossSalaryPerHour" class="control-label"></label>
                        <div class="input-group">
                            <input name="GrossSalaryPerHour" pattern="^\d*(\,\d{0,2})?$" class="form-control" value="@Model.GrossSalaryPerHour">
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="NetSalaryPerHour" class="control-label"></label>
                        <div class="input-group">
                            <input name="NetSalaryPerHour" pattern="^\d*(\,\d{0,2})?$" class="form-control" value="@Model.NetSalaryPerHour">
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="ContractMembershipId" class="control-label"></label>
                        <div class="input-group" style="width:100%">
                            <select class="ContractMembership_Select2" asp-for="ContractMembershipId" style="width:50%">
                                @if (Model.ContractMembership != null)
                                {
                    <option value="@Model.ContractMembershipId" selected="selected">@Model.ContractMembership.Name</option>}
                            </select>
                            <span asp-validation-for="ContractMembershipId" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="ContractTypeId" class="control-label"></label>
                        <div class="input-group" style="width:100%">
                            <select class="ContractType_Select2" asp-for="ContractTypeId" style="width:50%">
                                @if (Model.ContractType != null)
                                {
                    <option value="@Model.ContractTypeId" selected="selected">@Model.ContractType.Name</option>}
                            </select>
                            <span asp-validation-for="ContractTypeId" class="text-danger"></span>
                        </div>
                    </div>


                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <div class="input-group">
                            <textarea asp-for="Description" class="form-control"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>

                    <hr />

                    <div class="form-group pull-right">
                        <input type="submit" value="Αποθήκευση" class="btn btn-primary " />
                    </div>

                </div>
            </form>
        </div>
    </div>
    <div class="col-md-1"> </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}


    <script>
        //Decimal regex
        $(document).on('keydown', 'input[pattern]', function (e) {
            var input = $(this);
            var oldVal = input.val();
            var regex = new RegExp(input.attr('pattern'), 'g');

            setTimeout(function () {
                var newVal = input.val();
                if (!regex.test(newVal)) {
                    input.val(oldVal);
                }
            }, 1);
        });


        $('.ContractMembership_Select2').select2({
            allowClear: true,
            ajax: {
                url: '/api/ContractMemberships/select2',
                data: function (params) {
                    var query = {
                        search: params.term,
                        page: params.page || 1
                    }
                    return query;
                }
            }
        });

        $('.ContractType_Select2').select2({
            allowClear: true,
            ajax: {
                url: '/api/contractTypes/select2',
                data: function (params) {
                    var query = {
                        search: params.term,
                        page: params.page || 1
                    }
                    return query;
                }
            }
        });

        $('#HireDate').calentim({
            showTimePickers: false,
            startOnMonday: true,
            showHeader: true,
            showButtons: true,
            singleDate: true,
            startDate: Date.now(),
            endDate: Date.now(),
            format: "DD/MM/YYYY",
            calendarCount: 1,
            locale: "el",
            showOn: "right",
            onaftershow: () =>
                //Fix Calentim width
                [...document.getElementsByClassName('calentim-input')]
                    .forEach((calentim) =>
                        calentim.setAttribute('style', 'width: 400px;'))
        })
    </script>

}
